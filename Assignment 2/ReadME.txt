Assignment 2
Stuart Johnstone

to run this program compile either assign2a.s with gcc or demacro the assign2b.asm into assign2b.s then compile with gcc

To run this program execute the command ./a.out with no other inputs

you will be prompted to input a number between 5 and 20, if a different numb is input then it will ask again

it will then generate a list of random numbers that's length is equal to the length of the input

it then gives you the sum of the numbers, the largest frequency and the smallest frequency.


